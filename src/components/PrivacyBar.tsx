export function PrivacyBar() {
  return (
    <div class="privacybar" role="note">
      <span class="privacybar__dot" aria-hidden="true" />
      <span>
        <strong>Your files never leave this device.</strong> Everything runs in your browser —
        nothing is uploaded.
      </span>
    </div>
  )
}
